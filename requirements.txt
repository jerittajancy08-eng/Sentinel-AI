fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
openai==1.35.3
python-dotenv==1.0.1
httpx==0.27.0

# Optional — Microsoft Foundry Agent Service (agent threads, tools, orchestration).
# Sentinel AI works fully without these (falls back to Azure OpenAI / mock mode);
# install to enable the real Foundry Agent Service integration in
# core/foundry_agent_service.py.
azure-ai-projects==1.0.0b5
azure-ai-agents==1.0.0
azure-identity==1.17.1
